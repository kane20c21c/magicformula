# 황금률만들기 (MagicFormula) — 룰 레지스트리

이 리포는 **매매 룰 4개의 판단(로직) 소유자**다. 데이터는 longlivevault(LLV)가,
서빙·가상계좌는 StockPortfolio(SP)가 맡는다 — 이 분업은 바꾸지 않는다.

**작업 시작 전 이 파일을 먼저 읽는다.** 어떤 룰을 손대는지 확정한 뒤,
해당 룰 폴더의 스펙 문서로 들어갈 것. 룰끼리 파라미터·어휘를 섞지 말 것.

---

## 룰 4개

| # | 룰 | 폴더 | 스펙 정본 | 성격 |
|---|---|---|---|---|
| 1 | **황금률** | `magic_formula/` | `configs/active_strategy.yaml` | 진입신호 (종목 점수) |
| 2 | **데이 포트** | `mop_model/` | `mop_model/CLAUDE.md` | 1일 스윙 (ML) |
| 3 | **스윙 포트** | `korean_mkt_study/` | `STRATEGY.md` + `strategy_spec.json` | 눌림목 진입 |
| 4 | **발열률** | `fever_model/` | `fever_model/CLAUDE.md` | 시장온도 + 관측 명단 |

### 1. 황금률 — `COMBINED-v2-2026-05`
4영역 robust 가중 결합 **추세 20 / 모멘텀 20 / 거래량 0 / 변동성 60**
+ **와이코프 게이트**(Markdown 국면 매수 제외 — 리포트 ⛔).
진입 = 종합점수 **6.0 상향돌파**(`threshold_breakout`), 체결 다음날 시가.
청산 = 진입가 −ATR(14)×1 손절 / 20거래일 + 누적손익 ≤0% 시간청산.

⚠ 와이코프는 **5번째 점수 요소가 아니라 게이트**다. 데이·스윙 포트는 황금률의
입력이 아니다 (독립 룰).
⚠ 정본은 언제나 `configs/active_strategy.yaml`. (`SCHEDULED_TASKS.md` 에 남아 있던
v1 라벨 "CompR15+R1" 은 2026-08-14 `v2_combined` 로 수정 완료.)

### 2. 데이 포트 — MOp ML
16:20 신호 → 17:00 NXT 매수 → 직전 관측가 −2% 손절.
신호 `p` 는 확률이 아니라 **백분위**(충원 하한 `min_signal_p=0.925`).
15:10 섹터 오버나이트 게이지 별도(`run_gauge.py`, **10세트 75종목 · Kane 고정가중**
— 2026-08-15 개편, 정본 `mop_model/src/gauge_config.py`).

### 3. 스윙 포트 — `kr_pullback_largecap_foreign` **v1.2.0** (2026-08-16)
유니버스 = 월말 시총 5조↑ & 외국인 30%↑ (실사용 파일은 **4조/25%**). 진입 =
120일선 위 + 60일 고점 대비 10%↑ 눌림(onset만). 20슬롯 × 500만.
**진입 규칙은 v1.0.0 잠금 그대로** — 바뀐 건 청산뿐이다.

손절 = **시간 연동 4분기 × 종목 변동배율** (v1.2.0):
```
배율 = 종목 YZ_20 ÷ 직전 252거래일 스윙풀 YZ_20 중앙값의 중앙값
피크>평단 D+0~5 −20%×배율(클립 10~40%) / D+6~ −5%×배율(3~15%)
피크≤평단 D+0~5 −20%×배율(10~40%)     / D+6~ −10%×배율(5~25%)
```
⚠ **분모가 '그날 시장 중앙값'이 아니라 '직전 1년 중앙값'인 게 핵심.** 상대 척도로
쓰면 폭락장에서 배율이 1.0 으로 눌려 포트가 바닥에서 동시에 털린다(2020~21 구간
+0.2% vs +21.2%). 배율 정본은 LLV `data/vol_scale.json` (평일 20:50 배치).
⚠ 청산 정본은 v1.1.0 부터 **실거래 엔진**(`StockPortfolio/app/paper/config.py`)이다 —
`strategy_spec.json` 의 `trail_pct: 0.20` 은 백테스트 원안 값이지 운영값이 아니다.
⚠ 전략 변경 전 `STRATEGY.md` §11 기각목록 + §4 근거를 먼저 읽을 것.
⚠ **규칙 비교는 라이브 조건(t+1 시가·왕복 0.23%)으로 할 것.** §8 백테스트 관례
(종가·0.6%)로 재면 고회전 규칙이 부당하게 불리해진다(실측 1.33억 → 2.00억).

### 4. 발열률 — 2026-08-14 명명·구축
① 나우캐스트 시장온도(W60/M4)로 장의 열기 측정 → ② Vblk 탄력점수(33:17:17:33)로
그 안에서 열을 내는 종목 선별. 관측 지평 **20~60거래일**(스윙 포트와 같은 주기).
평일 16:30 산출 → 17:00 메일 + 8501 페이지. 상세는 `fever_model/CLAUDE.md`.

⚠ **매수 신호가 아니라 관측 명단**이다 — 황금률·스윙·데이 포트와 달리 진입·청산
규칙이 없다. 급등장에선 순위가 베타(YZ) 순서로 쏠린다.

---

## 스케줄 (평일, KST — 정본은 `StockPortfolio/SCHEDULED_TASKS.md`)

| 시각 | 룰 | 내용 |
|---|---|---|
| 15:10 | 데이 | MOp 섹터 오버나이트 게이지 |
| 16:20 | 데이 | 익일 매수 시그널 산출 |
| 16:30 / 16:35 | 황금률 | 점수 계산 / 확장 시그널 |
| 16:30 | **발열률** | 일일 워크포워드 (온도·Vblk 산출) |
| 16:45 | 스윙 | 신호 생성 (조용히) |
| 17:00 | 황금률 · 데이 · **발열률** | 신호 이메일 / 데이 청산·정리 / **발열률 메일** |
| 17:02 | 스윙 | 리마인드 알림 |
| 20:40 / 20:45 | 황금률 | 수급 반영 후 재산출 |
| 20:45 | 스윙 | 신호 백업 |

## 확인 화면

- **황금률** → 아웃퍼포머 `localhost:8501` **Quickview**
  (상단 진입신호(≥6.0)/후보(≥5.0) 카운트, 4영역 개별 점수, 국면·전환신호, 4셀 배지)
- **데이 포트** → 투자포폴 `localhost:8000` 데이 포트 페이지
- **스윙 포트** → 투자포폴 `localhost:8000` 스윙 포트 페이지
- **발열률** → 아웃퍼포머 `localhost:8501` **Temp.View**(발열게이지) 페이지 + 평일 17:00 메일

## 공통 규칙

- 판단 로직은 MagicFormula 소유, LLV 는 데이터 공급자지 판단자가 아니다.
- 슬롯 사이징은 **수수료 포함 총지출 기준** (`slot_krw // price` 는 버그).
- 숫자 표기: 원 = 소수점 없이 `x,xxx,xxx원`, 지수 = `xx.xx`, 변화율 = `xx.x%`,
  상승 `#ef5350`(빨강) / 하락 `#1976D2`(파랑), 우측 정렬.
