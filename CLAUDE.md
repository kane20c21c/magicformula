# 황금률만들기 (MagicFormula) — 룰 레지스트리

> **⚠ 작업 경계 — StoLab 절대 규칙 7 (2026-08-26)**
> 어시스턴트는 **수정 → 스모크 테스트 → `git commit`** 까지만 한다. 테스트 없이 커밋하지 않는다.
> `git push` · 미니 `git pull` · 서버 재구동(`launchctl`) · `~/DriveForALL/StoLab` 쓰기는
> **케인이 직접** 한다 — 어시스턴트는 명령어를 제시만 하고 실행하지 않는다.
> 상세는 `StoLab/CLAUDE.md` 의 "작업 경계" 절. **이 파일에 사본을 만들지 말 것** (드리프트 방지).

이 리포는 **매매 룰 4개의 판단(로직) 소유자**다. 데이터는 longlivevault(LLV)가,
서빙·가상계좌는 StockPortfolio(SP)가 맡는다 — 이 분업은 바꾸지 않는다.

**작업 시작 전 이 파일을 먼저 읽는다.** 어떤 룰을 손대는지 확정한 뒤,
해당 룰 폴더의 스펙 문서로 들어갈 것. 룰끼리 파라미터·어휘를 섞지 말 것.

---

## 룰 4개

| # | 룰 | 폴더 | 스펙 정본 | 성격 |
|---|---|---|---|---|
| 1 | **황금률** | `magic_formula/` | `configs/active_strategy.yaml` | 진입신호 (종목 점수) |
| 2 | **데이 포트** | `mop_model/` | `mop_model/CLAUDE.md` | 1일 스윙 (ML) |
| 3 | **스윙 포트** | `korean_mkt_study/` | `STRATEGY.md` + `strategy_spec.json` | 눌림목 진입 |
| 4 | **발열률** | `fever_model/` | `fever_model/CLAUDE.md` | 시장온도 + 관측 명단 |

### 1. 황금률 — `COMBINED-v3-2026-08` (2026-08-25~)
4영역 가중 결합 **추세 0 / 모멘텀 80 / 거래량 0 / 변동성 20**
+ **와이코프 게이트**(Markdown 국면 매수 제외 — 리포트 ⛔).
진입 = 종합점수 **6.0 상향돌파**(`threshold_breakout`), 체결 다음날 시가.
청산 = 진입가 −ATR(14)×1 손절 / 20거래일 + 누적손익 ≤0% 시간청산.

```
종합점수 = 0.8 × (RSI(14) − 50)/5
         + 0.2 × [ 0.6 × (52주위치 − 0.5)×20 + 0.4 × BB꺾임점수 ]
```

**v2 → v3 에서 바뀐 것** (2026-08-25, 근거 = SP `reports/황금률_전구간_재검증_20260824.html`)

| | v2 (~2026-08-25) | v3 |
|---|---|---|
| 변동성 | BB×52주×레짐 **점수표** (52주 저점에 최고점) | 52주위치 0.6 + BB꺾임 0.4, **레짐 미사용** |
| 모멘텀 | RSI 5-band **계단** (≥90 에 +10) | `(RSI−50)/5` **연속 선형** |
| 가중치 | T20/M20/Vu0/Va60 | **T0/M80/Vu0/Va20** |
| 임계 | 6.0 / 5.0 | **그대로** |

상위 3% 20일 상대수익(코어 67): **−1.29% (0/4 연도) → +4.30% (4/4)**, 둘 다 p<0.001.
실운용(슬롯10·t+1시가·왕복0.43%): 누적 +118% → **+400%**, MDD −40.7% → **−19.8%**,
Sharpe 0.96 → **1.87**. 검증 하네스 = `scripts/validate_area_redesign.py`.

⚠ **이 점수는 예측 모델이 아니라 상위 선별기다.** 20일 상대수익과의 순위상관(IC)은
+0.007(p=0.25)로 유의하지 않다 — 이득이 10분위 중 최상위에만 몰려 있다.
"점수가 높을수록 수익이 좋다"고 읽지 말 것. (v2 는 IC −0.015, p=0.0004 로 **유의하게
음수**였다. v3 는 음수를 0 근처로 끌어올린 것이지 예측력을 확보한 것이 아니다.)
⚠ **추세·거래량은 가중 0 이라 종합점수에 안 들어간다.** Quickview 4영역 배지에는
계속 보이지만 뒤 둘은 참고값이다. 추세를 되살리면 단조 악화(MDD −18.7→−29.4%).
⚠ **v3 는 2026-07·08 두 달간 코어 신호가 0 이다.** 폭락 구간에서 현금으로 빠지는
것이 설계 의도지만(현금비중 51%, 신호 있는 날 56%), 신호가 없다고 고장 난 게 아니다.
⚠ 검증 수치는 전부 **205종목 현재 명단(생존편향) · 2023-05~2026-08 초강세장**에서
나왔다. point-in-time 재검증·약세장 백필 전에는 확정값으로 쓰지 말 것 (HANDOFF §5.1).
⚠ 롤백 = `configs/active_strategy_v2.yaml` 복원 + `score_momentum_step()` ·
`score_volatility_table()` 로 되돌리기 (두 함수 보존됨).

⚠ 와이코프는 **5번째 점수 요소가 아니라 게이트**다. 데이·스윙 포트는 황금률의
입력이 아니다 (독립 룰).
⚠ 정본은 언제나 `configs/active_strategy.yaml`. (`SCHEDULED_TASKS.md` 에 남아 있던
v1 라벨 "CompR15+R1" 은 2026-08-14 `v2_combined` 로 수정 완료.)

### 2. 데이 포트 — MOp ML **v1.1.2.2** (2026-08-24~)
버전 = 모델.유니버스.진입.청산 (스윙 포트와 동일 체계 — 2026-08-23 Kane 도입).
16:20 신호 → 17:00 NXT 매수 **10슬롯 × 2백만** (p ≥ 0.925 & rank ≤ 15 충원, NXT 미거래 Pass)
→ 청산 = **당일고점 −1% 트레일링** + 첫 관측 갭 관문 −5%.
설정 정본 = SP `app/paper_day/config.py` + `data/paper_day/config.json` 오버라이드
(10슬롯 전환은 오버라이드로만 — 코드 기본값은 5×4백만 유지).
신호 `p` 는 확률이 아니라 **백분위**(충원 하한 `min_signal_p=0.925`).
15:10 섹터 오버나이트 게이지 별도(`run_gauge.py`, **10세트 75종목 · Kane 고정가중**
— 2026-08-15 개편, 정본 `mop_model/src/gauge_config.py`).

이력: v1.1.1.1 (07-27 개시, 5슬롯×4백만·청산 −2%) → v1.1.1.2 (07-30 청산 −1%)
→ **v1.1.2.2** (08-24 10슬롯×2백만 — 첫 4주 검증: rank 6~15 우위 + 분산 효과).
**그림자 3규칙 병행 추적** (평일 20:55 `StockPortfolio/scripts/day_shadow_track.py`,
상태 `SP/data/paper_day/shadow/`, 1개월 후 비교 — Kane 지시 2026-08-23):
R1 **v1.1.1.2** 구진입 5×4백만 / R2 **v1.1.3.2** 10×2백만+섹터캡3 / R3 **v1.1.2.1** 청산 −2%.
진입가 = KIS NX 당일 17:00 분봉 소급, 청산 = UN 분봉 10분 감시 재현 (실체결 대비 재현 상관 0.963).
근거 리포트: SP `reports/데이포트_{첫4주_성과분석,개선검증,매도타이밍_손절임계}_20260823.html`
— 손절 임계는 1.5/2.0% 모두 비유의·스윕 비단조라 1% 유지 (08-17 변동배율 기각과 정합).
⚠ 프리미엄 게이트·발열 게이트는 검증 후 보류 (게이트 임계 비단조 / 발열 익일 예측력 0).

### 3. 스윙 포트 — `kr_pullback_largecap_foreign` **v1.2.2.3** (2026-08-17)
버전 = 모델.유니버스.진입.청산 (설정 1, 조정마다 +1 — Kane 체계).
유니버스 = 월말 시총 **4조↑ & 외국인 30%↑** (2번째 — 완화는 네 방향 모두 단조 악화,
외인 필터는 위기 방어 필터). 진입 = **MA200 위 + 40일 고점 대비 10%↑ 눌림**(onset만,
2번째 = W1 — 평시 손실 ⅓, 상세 STRATEGY.md §3). 20슬롯 × 500만(백테스트) / 200만(가상계좌).
직전 모델 **v1.1.1.2** 는 그림자 추적 중 (`korean_mkt_study/shadow_track.py`,
평일 20:35 — 다음 달 모델 평가용). 규칙 실험은 `korean_mkt_study/backtest.py` 로.

손절 = **시간 연동 4분기 × 종목 변동배율** (v1.2.0):
```
배율 = 종목 YZ_20 ÷ 직전 252거래일 스윙풀 YZ_20 중앙값의 중앙값
피크>평단 D+0~5 −20%×배율(클립 10~40%) / D+6~ −5%×배율(3~15%)
피크≤평단 D+0~5 −20%×배율(10~40%)     / D+6~ −10%×배율(5~25%)
```
⚠ **분모가 '그날 시장 중앙값'이 아니라 '직전 1년 중앙값'인 게 핵심.** 상대 척도로
쓰면 폭락장에서 배율이 1.0 으로 눌려 포트가 바닥에서 동시에 털린다(2020~21 구간
+0.2% vs +21.2%). 배율 정본은 LLV `data/vol_scale.json` (평일 20:50 배치).
⚠ 청산 정본은 v1.1.0 부터 **실거래 엔진**(`StockPortfolio/app/paper/config.py`)이다 —
`strategy_spec.json` 의 `trail_pct: 0.20` 은 백테스트 원안 값이지 운영값이 아니다.
⚠ 전략 변경 전 `STRATEGY.md` §11 기각목록 + §4 근거를 먼저 읽을 것.
⚠ **규칙 비교는 라이브 조건(t+1 시가·왕복 0.23%)으로 할 것.** §8 백테스트 관례
(종가·0.6%)로 재면 고회전 규칙이 부당하게 불리해진다(실측 1.33억 → 2.00억).

### 4. 발열률 — 2026-08-14 명명·구축
① 나우캐스트 시장온도(W60/M4)로 장의 열기 측정 → ② Vblk 탄력점수(33:17:17:33)로
그 안에서 열을 내는 종목 선별. 관측 지평 **20~60거래일**(스윙 포트와 같은 주기).
평일 16:30 산출 → 17:00 메일 + 8501 페이지. 상세는 `fever_model/CLAUDE.md`.

⚠ **매수 신호가 아니라 관측 명단**이다 — 황금률·스윙·데이 포트와 달리 진입·청산
규칙이 없다. 급등장에선 순위가 베타(YZ) 순서로 쏠린다.

---

## 스케줄 (평일, KST — 정본은 `StockPortfolio/SCHEDULED_TASKS.md`)

| 시각 | 룰 | 내용 |
|---|---|---|
| 15:10 | 데이 | MOp 섹터 오버나이트 게이지 |
| 16:20 | 데이 | 익일 매수 시그널 산출 |
| 16:30 / 16:35 | 황금률 | 점수 계산 / 확장 시그널 |
| 16:30 | **발열률** | 일일 워크포워드 (온도·Vblk 산출) |
| 16:45 | 스윙 | 신호 생성 (조용히) |
| 17:00 | 황금률 · 데이 · **발열률** | 신호 이메일 / 데이 청산·정리 / **발열률 메일** |
| 17:02 | 스윙 | 리마인드 알림 |
| 20:40 / 20:45 | 황금률 | 수급 반영 후 재산출 |
| 20:45 | 스윙 | 신호 백업 |

## 확인 화면

- **황금률** → 아웃퍼포머 `localhost:8501` **Quickview**
  (상단 진입신호(≥6.0)/후보(≥5.0) 카운트, 4영역 개별 점수, 국면·전환신호, 4셀 배지)
- **데이 포트** → 투자포폴 `localhost:8000` 데이 포트 페이지
- **스윙 포트** → 투자포폴 `localhost:8000` 스윙 포트 페이지
- **발열률** → 아웃퍼포머 `localhost:8501` **Temp.View**(발열게이지) 페이지 + 평일 17:00 메일

## 공통 규칙

- 판단 로직은 MagicFormula 소유, LLV 는 데이터 공급자지 판단자가 아니다.
- 슬롯 사이징은 **수수료 포함 총지출 기준** (`slot_krw // price` 는 버그).
- 숫자 표기: 원 = 소수점 없이 `x,xxx,xxx원`, 지수 = `xx.xx`, 변화율 = `xx.x%`,
  상승 `#ef5350`(빨강) / 하락 `#1976D2`(파랑), 우측 정렬.
