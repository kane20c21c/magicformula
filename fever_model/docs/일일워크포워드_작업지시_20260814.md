# 일일 워크포워드 작업지시 — 2026-08-14 시작

케인 지시(8/13 밤): 오늘(8/13) 만든 결과들을 **매일 저녁 walk-forward**로 돌린다. 이 문서를 기반으로 구현할 것.

## 산출물 2개 (매일 종가 확인 후)

### 1) 시장온도 그래프 1장 — `나우캐스트_시장온도_W60M4_TIGER200.png`을 참고해서 그릴 것 
- 내용: **W60/M4 온도 + W100/M6 온도 + TIGER200(102110) 종가**를 한 그래프에
- 표시 구간: **최근 60거래일** (매일 한 칸씩 굴러감)
- 축: 좌 = 온도 %(0~100), 우 = TIGER200 종가(원)
- 색: W60/M4 빨강 `#d32f2f`(채택안, 굵게) / W100/M6 회색 `#9e9e9e` / TIGER200 검정 / 역사 중앙값 29% 점선 `#1976D2`
- 스프레드(W100−W60)가 이 그래프의 관전 포인트 (§5-6: 냉각 진행 신호 후보. 8/13 현재 W60 51% vs W100 55%)

### 2) Vblk 상위 10 종목
- **Vblk = (p1 + (p2n+p3n)/2 + p4) / 3** (8/13 채택, 33:17:17:33)
- D+0(당일)·D-1·D-2(거래일 기준) 세 시점의 상위 10을 한 표에:
  - 컬럼: 순위, 티커, 종목명, 섹터, 나우캐스트 국면(W60/M4), Vblk D-2/D-1/D+0, 순위 D-2/D-1/D+0, p4 단독순위, 과열, 재가열, 신규진입(↑)/이탈(↓) 표시
  - 각 시점 점수는 그 시점까지의 데이터만 사용(trailing이라 자동으로 point-in-time)
- 숫자 규칙: 점수 0.0, 주가 x,xxx원, 상승 빨강 `#ef5350`/하락 파랑 `#1976D2`, 우측 정렬

## 구현 설계 — `daily_WW_wf.py` (Weis Wave_walkforward 신규 1개)

### 입력
- LongLiveVault 내 데이터 사용: `glob("core.parquet")` / `glob("extend.parquet")` . 데이터 최신 거래일 = 판정일 D+0
- 고정 모델: `panel_states.csv` (스케일러+중심점, 전 기간 학습 캐비앗 유지)
- 제외 종목 : 우선주, 상장 일자가 2023년 이후 종목 등 제외

### 코드 재사용 (검증 끝난 로직 그대로)
- **나우캐스트 판정**: `nowcast_grid.py`의 `build_waves`/`vol_median_upto`/`features`(13특징) + 중심점 배정. W60은 gate `n_waves>=4`, W100은 `>=6`. 8/13 저녁 인라인 검증 코드와 동일 (온도 51%/55% 재현 확인됨)
- **탄력 점수**: `resilience_v2.py`의 `signals`(s1 최근8파동, s2n/s3n 점화부=창 후반 1/3, yz) + `score_at` → Vblk 추가. 중립 처리(점화부 쌍 없음 → 50) 포함. 과열/재가열 플래그는 `resilience_score.py` V1 로직 (탄력점수_V2 export 때 쓴 코드)

### 종목 모니터링을 위해 독립적인 데이터 저장 필요 (append, 중복 날짜 skip)
- `나우캐스트_온도일지`: date, hot60, hot100, 장기60, 저변60, 단기60, 유보60, tiger_close
- `탄력점수_일지`: date, ticker, Vblk, rank, p1, p2n, p3n, p4, nowcast, climax, reheat
  → D-1·D-2는 일지에서 읽는다 (재계산 아님). **최초 실행 시 백필 필요**: 온도는 최근 60거래일 일간, 점수는 최근 3거래일

### 처리 순서 (매일)
1. 최신 parquet 쌍 탐지 → 최신 거래일 확인 → 일지에 이미 있으면 중단(중복 방지)
2. W60/M4·W100/M6 전 종목 판정 → 온도 산출 → 온도일지 append
3. Vblk 전 종목 점수 → 점수일지 append
4. 그래프 생성 (온도일지 최근 60거래일 + TIGER200)
5. 상위 종보 비교표 생성 (일지에서 D-2·D-1 로드)
6. 콘솔 요약: 온도 변화, 스프레드, 상위10 신규진입/이탈 종목명
* 주의 : 산출물을 전달할 경로에 대한 케인의 확인이 필요함

### 주의사항 (오늘 확정된 것들)
- 온도 절대 레벨은 캘리브레이션 의존(워크포워드 검증 §5-6) — 해석은 방향·상대 변화 위주
- 온도 5일 단위 변화는 노이즈(차분 검증) — 일간 등락에 의미 부여 금지, 추세로 읽기
- Vblk 상위 명단은 20~60거래일 관측 명단, 장기 보유 아님. 급등장에선 순위가 베타(YZ) 순서로 쏠림(8/13 OOS)
- 8/7 빈티지 채점(§5-7 ④): **9/4경(20거래일)·11월초(60거래일)** — 그날 `탄력점수_V2_채점_20260813.xlsx` 방식으로 재채점. 빈티지 명단은 그 파일 안에 보존돼 있음

### 백필 사양 (최초 1회)
- 온도: 최신 parquet로 최근 60거래일 **일간** 롤링 판정 (계산량: 60일×191종목×2W ≈ 수 분). 기존 `nowcast_grid_hot.csv`(5일 간격, ~8/7)와 겹치는 구간으로 검산
- 점수: 최근 3거래일(D-2, D-1, D+0)만

### 미결(내일 케인에게 확인 후 진행)
1. 실행 방식: 케인이 parquet 반입 후 "일일 워크포워드 돌려줘" 한 마디(반자동) vs Cowork 예약 작업(매일 저녁 자동 — 단 parquet 반입이 선행돼야 함)
2. 비교표에 D+0 종가·등락률 컬럼 추가 여부
3. 그래프에 스프레드(W100−W60) 보조 패널 추가 여부
4. 최종 산출물 전달할 경로 확인

## 참고 — 오늘(8/13) 기준값 (내일 검산용)
- W60/M4: 온도 51% (장기 92 / 저변 90 / 단기 1 / 유보 8), W100/M6: 55%
- TIGER200: 8/13 종가 107,530원 (8/7 98,280원, 4거래일 +9.41%)
- Vblk 상위(8/7 기준): 피에스케이, 테스, 심텍, OCI홀딩스, … (`탄력점수_V2_채점_20260813.xlsx` 참조)
- 전체 맥락: `파동국면_스터디노트_20260811.md` §5-6(나우캐스트 W60/M4 채택), §5-7(Vblk 채택·검증 사이클)
